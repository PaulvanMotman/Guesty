extends layout

block content
	div.container
		div.row
			div.col-sm-6.col-md-4.col-md-offset-4
				#user
					h1.text-center.login-title Welcome #{user.firstname}. Check your details below:
						div.signup-wall
							ul.user-details
								li Username ---> #{user.displayName}
								li Email    ---> #{user.email}
								li First Name ---> #{user.firstname} 
								li Last Name ---> #{user.lastname}
						div.signup-wall
							a(href='/createevent', class='text-center new-account') Add event	
						a(href='/signout', class='text-center new-account') Sign Out
				//		h1 This page displays all your own events #{user.name}.
				//			each event in events 
				//				#single
				//					h2.nameEvents
				//						a(href='/single-event/#{event.id}') Name: #{event.name}


